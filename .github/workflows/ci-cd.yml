name: CI/CD con Docker

on:
  push:
    branches:
      - main # Se activa cuando hay un push a la rama principal

jobs:
  build-test-scan:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout del código
        uses: actions/checkout@v4

      - name: ⚙️ Configurar Docker Buildx
        uses: docker/setup-buildx-action@v3

      # -------------------------------------------------------------------------
      # FASE CI: Construcción de Imagen (Solo para Buildx)
      # -------------------------------------------------------------------------
      - name: 🏗️ Construir la Imagen Docker (CI)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.txt
          tags: mi-app-test:latest
          push: false
          load: true # <--- ¡CLAVE! Esto asegura que la imagen se almacene localmente

      # -------------------------------------------------------------------------
      # FASE CI: Ejecutar Pruebas (Ajuste de Red y Tiempos)
      # -------------------------------------------------------------------------
      - name: 🧪 Ejecutar Pruebas de Integración (Usando Host)
        run: |
          # 1. Ejecutamos el servidor (Con la imagen cargada localmente gracias a load: true)
          docker run -d --rm --name test_server -p 3000:3000 mi-app-test:latest 
          
          # 2. Esperamos un momento inicial
          echo "Esperando a que el servidor responda en el host (localhost:3000)..."
          sleep 5
          
          # 3. Bucle de Health Check con tolerancia a fallos
          for i in {1..10}; do
            # CORRECCIÓN: Añadimos '|| echo "000"' para evitar que el script muera si curl falla
            response=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 || echo "000")
            
            if [ "$response" -eq 200 ]; then
              echo "✅ Prueba de conexión exitosa: Código 200. Servidor listo."
              # Detenemos el contenedor y salimos con éxito
              docker stop test_server
              exit 0
            fi
            
            echo "Intento $i/10 fallido (Código: $response). Reintentando en 2 segundos..."
            sleep 2
          done
          
          # Si llegamos aquí, falló
          echo "❌ El servidor no respondió después de 10 intentos."
          docker stop test_server
          exit 1
        working-directory: .
        
      # -------------------------------------------------------------------------
      # FASE CI: Escaneo de Seguridad (DevSecOps)
      # -------------------------------------------------------------------------
      - name: 🔒 Escanear Imagen con Trivy (Seguridad)
        # Usamos la imagen construida 'mi-app-test:latest'
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'mi-app-test:latest' 
          format: 'table'
          exit-code: '1' # Falla si encuentra CRITICAL o HIGH
          severity: 'CRITICAL,HIGH'

      # -------------------------------------------------------------------------
      # FASE CD: Entrega Continua (Subida a Docker Hub)
      # -------------------------------------------------------------------------
      - name: 🚀 Login y Push a Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          
      - name: 📦 Subir Imagen a Docker Hub
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.txt # Usamos .txt ya que lo mantuviste con esa extensión
          push: true
          # Etiqueta con el hash de Git para inmutabilidad y 'latest' para facilidad
          tags: ${{ secrets.DOCKER_USERNAME }}/mi-app-ci:v${{ github.sha }}, ${{ secrets.DOCKER_USERNAME }}/mi-app-ci:latest

      # -------------------------------------------------------------------------
      # FASE CD: Despliegue Continuo (Desactivado hasta que la CI pase)
      # -------------------------------------------------------------------------
      # - name: 🌍 Despliegue Continuo (CD) - (Simulado con SSH/Script)
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USERNAME }}
      #     key: ${{ secrets.SSH_KEY }}
      #     script: |
      #       echo "Iniciando despliegue en el servidor..."
      #       docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      #       docker pull ${{ secrets.DOCKER_USERNAME }}/mi-app-ci:v${{ github.sha }}
      #       docker stop mi-app-web || true
      #       docker rm mi-app-web || true
      #       docker run -d --name mi-app-web -p 80:3000 ${{ secrets.DOCKER_USERNAME }}/mi-app-ci:v${{ github.sha }}
      #       echo "Despliegue de la versión v${{ github.sha }} finalizado."