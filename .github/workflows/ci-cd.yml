name: CI/CD con Docker

on:
  push:
    branches:
      - main # Se activa cuando hay un push a la rama principal

jobs:
  build-test-scan:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout del código
        uses: actions/checkout@v4

      - name: ⚙️ Configurar Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🏗️ Construir la Imagen Docker (CI)
        # Construye la imagen, pero no la sube aún
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.txt
          tags: ${{ secrets.DOCKER_USERNAME }}/mi-app-ci:latest
          push: false # No subir todavía

      - name: 🧪 Ejecutar Pruebas dentro de un Contenedor
        run: |
          docker build -t mi-app-test:latest . # Reconstruimos para la prueba
          # Ejecutamos las pruebas. Si fallan, el job falla.
          docker run --rm mi-app-test:latest npm test
        working-directory: .

      - name: 🔒 Escanear Imagen con Trivy (Seguridad)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'mi-app-test:latest' # Escanea la imagen recién construida
          format: 'table'
          exit-code: '1' # Falla si encuentra vulnerabilidades críticas
          severity: 'CRITICAL,HIGH'

      - name: 🚀 Login y Push a Docker Hub (CD - Entrega)
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          
      - name: 📦 Subir Imagen a Docker Hub
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true # Ahora sí, la subimos
          tags: ${{ secrets.DOCKER_USERNAME }}/mi-app-ci:v${{ github.sha }} # Tag con el hash de Git para inmutabilidad

      - name: 🌍 Despliegue Continuo (CD) - (Simulado con SSH/Script)
        # ESTE ES UN PASO SIMULADO. Debes adaptarlo a tu servidor.
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            echo "Iniciando despliegue en el servidor..."
            docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
            docker pull ${{ secrets.DOCKER_USERNAME }}/mi-app-ci:v${{ github.sha }}
            docker stop mi-app-web || true
            docker rm mi-app-web || true
            docker run -d --name mi-app-web -p 80:3000 ${{ secrets.DOCKER_USERNAME }}/mi-app-ci:v${{ github.sha }}
            echo "Despliegue de la versión v${{ github.sha }} finalizado."